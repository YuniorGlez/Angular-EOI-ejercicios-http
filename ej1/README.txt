Ejercicio 1:

        Durante estos ejercicios vamos a trabajar con una nueva factoría.

        Si te fijas, en la carpeta de public/js/factories/ ahora tenemos dos ficheros.

        En uno mantenemos la factoría de customers que trabaja con localstorage

        Y la nueva es la que realizaremos durante estos ejemplos.

        En este ejercicio, ve al controlador CRMController y reemplaza la factoría que se usa en el por la de http

        Implementa el método getAll de manera  que devuelva la variable customers que está declarada arriba. 

        Esto es solo para ver que funciona y que has cambiado bien la factoría :) en el siguiente ejercicio ya haremos el getAll bien y con http

        Si lo has hecho bien tienen que aparecer en la pantalla los 3 customers